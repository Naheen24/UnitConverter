<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.naheen.unitconverter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/unitconverter_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/unitconverter_app_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UnitConverter">
        <activity
            android:name=".MenuActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"
            android:exported="true"
            android:parentActivityName=".MenuActivity">
        </activity>
        <activity android:name=".GmToKgActivity"
            android:exported="true"
            android:parentActivityName=".MenuActivity">
        </activity>
        <activity android:name=".MeterToCmActivity"
            android:exported="true"
            android:parentActivityName=".MenuActivity">
        </activity>
        <activity android:name=".CmToMeterActivity"
            android:exported="true"
            android:parentActivityName=".MenuActivity">
        </activity>
    </application>

</manifest>